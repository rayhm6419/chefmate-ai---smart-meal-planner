server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chefmate
    username: your_db_username
    password: your_db_password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
